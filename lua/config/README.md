# `config`

Anything configuration related. 

All options applied by default are in `options` (except `theme`, since it is
likely to rely on plugins being loaded).

Anything that is `require`d from somewhere else is in `config`.
