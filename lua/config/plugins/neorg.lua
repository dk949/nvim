return {
    load = {
        ["core.defaults"] = {},
        ["core.concealer"] = {},
        ["core.dirman"] = {
            config = {
                workspaces = { notes = "~/Uni/notes", },
                default_workspace = "notes",
                open_last_workspace = false, -- maybe?
            },
        },
    }
}
